import { Dup2Dup1Dup3 } from "./src/output/extractedClasses/Child";

class Polymorphism {
    public smellyParent(dup1: number, dup2: number, dup3: number): void {

    }
}

class Child extends Polymorphism {
    public smellyChild(newParam: Dup2Dup1Dup3): void {
    }
}
